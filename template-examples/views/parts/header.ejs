<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <% if(title) { %>
    <title><%= title %></title>
    <% } else { %>
    <title>Page</title>
    <% } %>
    <link rel="stylesheet" href="style.css">
</head>
<body>